import React from 'react';

type MultipleFiltersProps = {
  category: string;
  onChangeFilter: (category: string, filterToApply: string) => void;
};

export const MultipleFilters = ({ category, onChangeFilter }: MultipleFiltersProps) => {
  return <div></div>;
};
